// Геометрия 3.
// На плоскости находятся K точек с координатами (X1, Y1), (X2, Y2), ..., (Xk, Yk). Найти среди них вершины квадрата, содержащего максимальное число заданных точек.
#include <iostream>
int main() {
	int k, sum, i;
	int square[4];
	int * points = new int[];
	std::cin >> k >> a[1];
	for (i = 0; i < k; k += 1) {
		std::cin >> points[i];
	}
	std::cout << square[0] << std::endl;
	std::cout << square[1] << std::endl;
	std::cout << square[2] << std::endl;
	std::cout << square[3] << std::endl;
}
